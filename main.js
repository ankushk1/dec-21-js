export const name = "ABC";
export const age = 20


export const arr = [1,2,3,4]

// export {
//   name,
//   age
// }

export const userInfo = (username, userage) => {
  console.log(`${username} age is ${userage}`);
}

